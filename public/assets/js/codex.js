$(document).ready(function() {

	// Bootstrap the tables
	$('table').addClass('table table-striped table-bordered table-hover table-condensed');

	// Prettyprint the <pre> tags
	$('pre').addClass('prettyprint theme-freshcut');

});
